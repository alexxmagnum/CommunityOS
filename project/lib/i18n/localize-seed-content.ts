/**
 * @deprecated Importar desde `@/lib/i18n/content`.
 * Re-export de compatibilidad — locale por defecto: español.
 */
export {
  localizeContent,
  localizeSeedContent,
  localizeFacilityRow as localizeFacility,
  localizeEventRow as localizeEvent,
  localizeMenuCategoryRow as localizeMenuCategory,
  localizeDishRow as localizeDish,
  localizeActivityRow as localizeActivity,
} from './content'
